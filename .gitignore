node_modules/
# Every .env variant is secret EXCEPT the committed template. Matching only
# `.env` and `.env.local` let files like `.env.backup` or `.env.production`
# through, which is how credentials get committed by accident.
.env*
!.env.example
dist/
build/
uploads/
*.log
.DS_Store
.claude


Chunks.md 
CLAUDE.md 
Handoff.md 
DEPLOYMENT_OPTIONS.md
AUTH.md
NxtGen Launch Technical Project Brief_ MBZUAI RLA Prepaid Card Reconciliation System.md
